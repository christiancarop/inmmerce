import React from "react";
import styles from "./index.css";

const HelloWorld = () => (
    <div className={`${styles.container}`}>
        <h1>home 1 desde react</h1>
    </div>
);

//HelloWorld.displayName = "helloworld";

export default HelloWorld;